@using FrontendFacturacion.Services

@{
    ViewData["Title"] = "Registrar Pago";

    var facturas = ViewBag.Facturas as List<FacturaDto> ?? new();
}

<div class="page-title">
    <div>
        <h2>Registrar Pago</h2>
        <p>Formulario preparado para registrar el pago de una factura.</p>
    </div>

    <a asp-controller="Pagos" asp-action="Index" class="btn btn-outline">Regresar</a>
</div>

<div class="form-card">
    <div class="form-section">
        <h3>Datos del pago</h3>

        <div class="grid grid-3">
            <div class="form-group">
                <label>Factura</label>
                <select>
                    @foreach (var factura in facturas)
                    {
                        <option value="@factura.IdFactura">@factura.NumeroFactura - @factura.Cliente</option>
                    }
                </select>
            </div>

            <div class="form-group">
                <label>Fecha de pago</label>
                <input type="date" value="@DateTime.Now.ToString("yyyy-MM-dd")" />
            </div>

            <div class="form-group">
                <label>Monto</label>
                <input type="number" value="150.00" step="0.01" />
            </div>

            <div class="form-group">
                <label>Método de pago</label>
                <select>
                    <option>EFECTIVO</option>
                    <option>TARJETA</option>
                    <option>TRANSFERENCIA</option>
                    <option>DEPOSITO</option>
                </select>
            </div>

            <div class="form-group">
                <label>Número de referencia</label>
                <input type="text" placeholder="Ejemplo: CAJA-01 o TRX-001" />
            </div>

            <div class="form-group">
                <label>Estado</label>
                <select>
                    <option>REGISTRADO</option>
                    <option>PENDIENTE</option>
                    <option>ANULADO</option>
                </select>
            </div>
        </div>
    </div>

    <div class="actions">
        <a asp-controller="Pagos" asp-action="Index" class="btn btn-outline">Cancelar</a>
        <button class="btn btn-primary">Guardar pago</button>
    </div>
</div>